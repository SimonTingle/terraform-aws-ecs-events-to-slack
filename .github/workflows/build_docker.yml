name: Build and Push Docker Image

on:
  release:
    types: [published]
  workflow_dispatch:

# --- PUTTING AWAY PER ANTON (2026-01-29) ---
# Reason: OIDC permissions will be handled via a different repository tomorrow.
# permissions:
#   id-token: write
#   contents: read

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v6

      # --- PUTTING AWAY PER ANTON (2026-01-29) ---
      # Reason: AWS Credential and ECR logic is being moved/reconfigured tomorrow.

      # - name: Configure AWS Credentials
      #   uses: aws-actions/configure-aws-credentials@v4
      #   with:
      #     role-to-assume: arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/github-ci-ecr-push-role
      #     aws-region: eu-central-1

      # - name: Login to Amazon ECR
      #   id: login-ecr
      #   uses: aws-actions/amazon-ecr-login@v2

      # - name: Build, tag, and push image to Amazon ECR
      #   uses: docker/build-push-action@v6
      #   with:
      #     context: ./functions
      #     file: ./functions/Dockerfile
      #     push: true
      #     tags: ${{ steps.login-ecr.outputs.registry }}/ecs-events-to-slack-repo:${{ github.event.release.tag_name }}

      - name: Build, tag, and push image to Amazon ECR
        uses: docker/build-push-action@v6
        with:
          context: ./functions
          file: ./functions/Dockerfile
          push: true
          tags: ${{ steps.login-ecr.outputs.registry }}/ecs-events-to-slack-repo:${{ steps.tag.outputs.image_tag }}